export interface ContactItem {
  available: string;
  createdAt: string;
  userId: string;
  contactId: string;
  name: string;
  address: string;
  phone: string;
  description: string;
  attachmentUrl?: string;
}
